import * as firebase from  "firebase/app";
import "firebase/auth";
var firebaseConfig = {
    //add the auth key from firebase authentication
  };
  firebase.initializeApp(firebaseConfig);
  export default firebase